# Allow newer Streamlit releases so the resolver can pick a combination
# that pulls a prebuilt Pillow wheel compatible with the deployment Python.
# Constraining upper bound keeps major-version compatibility.
streamlit>=1.26.0,<1.30
pandas
numpy
scikit-learn
joblib
redis
requests
# (No explicit Pillow pin here — allow resolver to pick a compatible wheel)
